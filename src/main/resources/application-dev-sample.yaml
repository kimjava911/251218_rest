spring:
  datasource:
    # https://console.aiven.io/
    url: jdbc:mysql://mysql-{}.aivencloud.com:{}/defaultdb?ssl-mode=REQUIRED
    username: avnadmin
    password: AVNS_{}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

file:
  upload-dir: ./uploads

# (6)-1
cors:
  allowed-origins: http://127.0.0.1:5500,http://localhost:5500,http://localhost:3000

springdoc:
  swagger-ui:
    enabled: true  # 기본값이 true이므로 생략 가능